node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Persisted application data (orders, menu, users, loyalty points, etc.)
# This is your restaurant's live database — do not delete in production!
data/
